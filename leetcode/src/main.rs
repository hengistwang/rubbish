use std::cmp::max;
use std::cmp::min;
use std::collections::BinaryHeap;
use std::collections::HashMap;
use std::collections::HashSet;
use std::collections::VecDeque;
fn main() {
    let s1 = String::from("sea");
    let s2 = String::from("ate");
}

struct Solution;
